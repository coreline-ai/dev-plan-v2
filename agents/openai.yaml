interface:
  display_name: "Codex Dev Plan"
  short_description: "Create phased plans with fail-closed model routing"
  default_prompt: "Use $codex-dev-plan-orchestrator to determine PLAN, EXECUTE, RESUME, QA, or STATUS. For execution, require explicit Sol/Terra/Luna routing and do not modify code or delegate unless execution is explicit."
