interface:
  display_name: "Codex Dev Plan"
  short_description: "Create concise dev plans and guide explicit implementation"
  default_prompt: "Use $codex-dev-plan-orchestrator to determine PLAN, EXECUTE, RESUME, QA, or STATUS. Do not modify code or delegate unless execution is explicit."
